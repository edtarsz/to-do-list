model List {
    id       Int    @id @default(autoincrement())
    name     String
    color    String

    userId   Int    
    user     User   @relation(fields: [userId], references: [id])

    // Relacion de uno a muchos con Task
    tasks  Task[] @relation("ListTasks")

    @@map("lists")
}